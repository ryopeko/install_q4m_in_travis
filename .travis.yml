language: ruby
rvm:
  - 2.1
before_install:
  - sudo apt-get install mysql-server libmysqlclient-dev
  - mkdir ${HOME}/mysql
  - cd ${HOME}/mysql
  - apt-get source mysql-server
  - ls
script: echo 'foo'
